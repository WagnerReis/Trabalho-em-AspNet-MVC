@model Marcenaria.Models.ItemCompra
@{
    ViewBag.Title = "Edicao da Item Compra";
}


@Html.ValidationSummary(true)
<div class="panel panel-warning">
    <div class="panel-heading">Edição de Item Compra</div>
    <p></p>
    @using (Html.BeginForm("Edit", "ItemCompra",
    FormMethod.Post, new { @class = "form-horizontal", @role = "form" }))
    {
        <div class="form-group">
            @Html.Label("Quantidade", new { @class = "col-sm-2 control-label" })
            @Html.TextBox("Quantidade", Model.Quantidade, new { @class = "form-control", type = "number", required = "false" })
        </div>
        <div class="form-group">
            @Html.Label("Valor", new { @class = "col-sm-2 control-label" })
            @Html.TextBox("Valor", Model.Valor, new { @class = "form-control", type = "number", required = "true" })
        </div>

        <p></p>

        <div class="row">
            <div class="col-md-4 col-md-offset-5">
                <div class="btn-group btn-group-sm">
                    <button type="submit" class="btn btn-default">Salvar</button>
                    <a href="@Url.Action("Index", "ItemCompra")" class="btn btn-default btn-sm">Voltar</a>
                </div>
            </div>
        </div>


    }
</div>

